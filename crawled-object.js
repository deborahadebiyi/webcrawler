

// returns a multi-dimensional array, first element of each array
// is the visited url 
// returns an object

const crawledUrls = {
    visitedUrl: [
      'links present',
      'on page' 
    ],
    nextVisitedUrl: [
        'more links present', 
        'on page'
    ]
}